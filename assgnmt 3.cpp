#include<iostream>
#include<cstdio>
using namespace std;
int main(){
	
    char str[8];

	gets(str);
	puts(str);
    
    if(str== 0 && str== 0 && str== 0   ){
	if(str[1]>=65 && str[1]<91){
		
	}
	}
	return 0;
}