#include<iostream>
int main(){
	
	int a[15];
	for(i=0;i<n;i++){
		cin>>a[i];
		
	}
	maximum= max(int a,int low,int high);
	cout<<maximum;	
	
}
 maxSubarray(int a,int low,int high){
 	
     if(low==high){
		 return a[high];
	 }else{
		  mid =(low+high)/2;
		 maxSubarray(a,low,mid);
		 maxSubarray(a,mid+1,high);
		 maxcrossingSubary(a,low,mid,high);
		 
	 } 
	 return max():
 }
 maxcrossingSubary(){
	 
	 
	 
	 
	 
	 
	 
	 
	 
	 
	 
	 
	 
 }